import React, { Component } from 'react';

const CustomScript = () => {
  useEffect(() => {
    // JavaScript code to execute after component mounts
    console.log('Component mounted, running JavaScript code...');
  }, []); 
}
export default CustomScript;
